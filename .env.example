# Shared Google tag ID (for example GT-XXXXXXX or G-XXXXXXXXXX)
NEXT_PUBLIC_GOOGLE_TAG_ID=

# Backward-compatible fallback for existing deployments still using the GA4 env name.
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Optional Google Ads conversion ID (for example AW-XXXXXXXXX).
NEXT_PUBLIC_GOOGLE_ADS_ID=
